// Database
export const DB = Object.freeze({
  NAME: "fastify-social",
});


// Security
export const SECURITY = Object.freeze({
  SALT_ROUNDS: 12,
});


// Cloud
export const CLOUD_FOLDERS = Object.freeze({
  MAIN: "fastify-social",
  PROFILE: "profile",
});